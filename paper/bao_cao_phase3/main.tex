\documentclass[11pt,a4paper]{article}
\usepackage[T5]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[vietnamese]{babel}
\usepackage{tgtermes}
\usepackage[margin=2.3cm]{geometry}
\usepackage{booktabs,graphicx,xcolor,array}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=blue!45!black, urlcolor=blue!45!black,
            bookmarksnumbered=true, bookmarksopen=true,
            pdftitle={Bao cao tien do Phase 3}, pdfauthor={Trung Kien}}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.6em}{}
\setlength{\parskip}{0.35em}
\setlength{\parindent}{0pt}
\newcommand{\up}[1]{\textcolor{teal!70!black}{\textbf{#1}}}
\newcommand{\down}[1]{\textcolor{red!65!black}{#1}}

\title{\vspace{-1.2cm}\textbf{Báo cáo tiến độ Phase 3}\\[0.25em]
\large Ứng dụng AI trong gán nhãn ảnh y tế cho bệnh đau thắt lưng}
\author{Trung Kiên \quad\textbar\quad GVHD: Phan Trọng Nhân}
\date{21/09/2026}

\begin{document}
\maketitle
\vspace{-0.9cm}


\vspace{0.3em}
\hrule
\vspace{0.6em}
{\small\tableofcontents}
\vspace{0.4em}
\hrule
\vspace{0.8em}

\section{Tóm tắt}

Báo cáo tổng hợp kết quả Phase 3, đối chiếu với mốc đã trình bày trước hội đồng ở Phase 2.
Ba nhóm việc đã hoàn thành: (i) phần mềm gán nhãn, trong đó \textbf{cơ chế} của vòng phản hồi
bác sĩ đã được cài đặt và kiểm thử đầy đủ nhưng \textbf{chưa từng chạy trên chỉnh sửa thật},
nên báo cáo này không đưa ra con số trước/sau nào cho nó (mục~3.7); (ii) bốn thực nghiệm GPU
tìm hướng cải thiện lớp Severe; (iii) chẩn đoán nguyên nhân điểm yếu ở nhãn lỗ liên hợp và
một khung đánh giá khả diễn giải có kiểm chứng.

Kết quả đáng kể nhất là \textbf{chẩn đoán được nguyên nhân} khiến nhãn lỗ liên hợp
(\textit{neural foraminal narrowing}) luôn kém: quy trình cắt ảnh cũ lấy vùng ảnh từ chuỗi
Sagittal T2 trong khi bác sĩ chấm nhãn này trên Sagittal T1, và đo trong không gian bệnh nhân
cho thấy \textbf{32{,}2\% (trái) và 47{,}2\% (phải)} vùng cần chấm nằm ngoài độ sâu ảnh
mà mô hình được cho. Sau khi cắt riêng theo từng bên trên T1, AUPRC lỗ liên hợp
tăng \up{+0{,}054} và \up{+0{,}039} so với Phase 2.

Báo cáo cũng ghi lại bốn kết quả âm và hai lỗi phương pháp đã phát hiện, vì chúng ảnh hưởng
tới cách diễn giải số liệu cũ.

\section{Phần mềm gán nhãn}

Ứng dụng web (FastAPI + React/Cornerstone3D) cho phép tải ảnh MRI, xem theo lát, chạy mô hình
chấm 11 nhãn bất thường, và \textbf{ghi nhận chỉnh sửa của bác sĩ như phản hồi chuyên gia}.
Quy mô hiện tại: 16.426 dòng Python, 2.452 dòng frontend, 24 tệp kiểm thử.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{figures/app_worklist.png}
\caption{Màn hình danh sách ca. Mỗi bệnh nhân có thể chứa nhiều lần chụp; trạng thái
\textit{Ready} nghĩa là ảnh đã tải xong và sẵn sàng chấm.}
\label{fig:worklist}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{figures/app_viewer.png}
\caption{Màn hình đọc ảnh, hiển thị chuỗi Sagittal T2 (512$\times$512$\times$17,
0{,}586$\times$0{,}586$\times$3{,}3 mm). Bảng \textit{Abnormality grades} nằm dưới ảnh cho phép
bác sĩ sửa từng nhãn; khối \textit{Doctor feedback} bên phải gom các chỉnh sửa thành hàng đợi
huấn luyện.}
\label{fig:viewer}
\end{figure}

Điểm thiết kế đáng lưu ý ở Hình~\ref{fig:viewer}: chỉnh sửa của bác sĩ \textbf{không tác động
ngay} tới mô hình. Chúng được xếp hàng, việc huấn luyện lại chạy ngoại tuyến, và phiên bản mô hình
mới phải được duyệt rồi mới kích hoạt. Điều này tránh tình huống kết quả của bệnh nhân sau bị
thay đổi bởi chỉnh sửa của bệnh nhân trước.

\section{Vòng phản hồi bác sĩ: phương pháp}

Mục~2 mô tả vòng phản hồi ở mức thao tác. Mục này nói phần phương pháp: cơ chế cập nhật là gì,
gọi tên thế nào trong tài liệu, đo bằng gì, và hiện đã làm tới đâu.

\subsection{Gọi tên cho đúng}

Hệ thống là \textbf{human-in-the-loop continual learning} dạng \textbf{batch/ngoại tuyến},
kịch bản \textbf{new-instance (NI)} --- cùng bài toán chấm điểm, cùng không gian nhãn, chỉ thêm
mẫu mới đã được bác sĩ hiệu chỉnh --- cập nhật bằng \textbf{rehearsal (replay)} và có
\textbf{cổng phê duyệt của người}.

Bóc từng vế: kịch bản NI theo bộ ba NI/NC/NIC của Lomonaco và Maltoni (CORe50, CoRL 2017), vì
không thêm lớp mới và không thêm tác vụ mới; nhãn cố định \textit{Normal/Mild--Moderate--Severe}
nên không phải class-incremental theo phân loại của van de Ven và Tolias (arXiv:1904.07734);
vai trò kép của bác sĩ --- vừa sinh nhãn vừa gác cổng --- là định nghĩa human-in-the-loop trong
khảo sát của Budd, Robinson và Kainz (\textit{Medical Image Analysis}, 2021).

Ba cách gọi cần tránh, vì đều sai và đều làm yếu lập luận. Thứ nhất, \textbf{không} gọi là
\textit{online learning}: online là cập nhật ngay theo từng ca, còn ở đây cố ý gom lô và chạy
ngoài giờ --- chính cái ``cố ý không'' đó mới là đóng góp. Thứ hai, \textbf{không} gọi trần là
\textit{fine-tuning}: fine-tune trần trên vài chục ca là \textit{iterative fine-tuning}, chưa phải
continual learning, vì thiếu cơ chế chống quên; thứ nâng nó lên là ba chốt ở mục~3.2. Thứ ba,
\textbf{không} nhận là \textit{active learning}: active learning là mô hình tự chọn ca nó không
chắc để hỏi, còn ở đây bác sĩ tự chọn ca. Đây là phần duy nhất trong phân loại còn thiếu, và nó
rẻ vì đầu ra đã có xác suất; xếp vào hướng phát triển.

\subsection{Cơ chế cập nhật và ba chốt an toàn}

Mô hình gồm hai phần: phần nền ResNet34-3D kèm CBAM (khoảng 63 triệu tham số, ``học cách nhìn'')
và ba đầu phân loại (khoảng 50 nghìn tham số, ``học cách phán''). Với vài chục ca chỉnh sửa,
huấn luyện 63 triệu tham số sẽ học thuộc đúng mấy ca đó và quên gần mười nghìn ca RSNA.

Vì vậy cách cập nhật là \textbf{chỉ huấn luyện ba đầu, đóng băng phần nền}. Giả định nền là bác sĩ
sửa nhãn phần lớn vì \textit{ngưỡng phán} lệch chứ không phải vì mô hình \textit{nhìn} sai; tỉ lệ
50 nghìn tham số trên vài chục mẫu cũng là tỉ lệ chấp nhận được. Kèm theo là ba chốt, mỗi chốt
chặn một kiểu hỏng không phát ra lỗi:

\begin{enumerate}
\itemsep0.15em
\item \textbf{Đóng băng BatchNorm.} Đóng băng phần nền chỉ tắt gradient, trong khi BatchNorm cập
nhật thống kê \textit{không} qua gradient. Một lô bốn mẫu sẽ đè thống kê tích luỹ từ toàn bộ tập
huấn luyện. Đây là nguyên nhân đã được ghi nhận của việc quên thảm khốc khi thích nghi mô hình y tế
trên tập nhỏ (arXiv:2011.08096).
\item \textbf{Bộ đệm replay.} Trộn khoảng 200 đĩa đệm gốc vào mỗi lượt cập nhật. Không có nó, mô
hình chỉ thấy những ca bị sửa và tưởng dữ liệu thật đều như vậy.
\item \textbf{Cổng macro-F1.} Chỉ giữ trọng số mới nếu macro-F1 tăng trên tập đánh giá đã đóng băng.
\end{enumerate}

Hình~\ref{fig:feedback_loop} vẽ toàn bộ vòng lặp theo bốn dải: trực tuyến (ứng dụng phục vụ), thu
thập (tách ``lưu'' khỏi ``gửi''), ngoại tuyến (huấn luyện lại theo lô và kiểm chứng), và triển khai
(người duyệt, có thể hoàn tác). Nét liền là thành phần đã cài đặt và đã kiểm thử; nét đứt là dự
kiến --- hiện chỉ có hai hộp nét đứt, là \textit{active learning} và \textit{kiểm tra chất lượng
nhãn}.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/feedback_loop_tikz.pdf}
\caption{Vòng lặp phản hồi bác sĩ theo cơ chế human-in-the-loop continual learning dạng batch.
Nét liền: thành phần đã cài đặt và kiểm thử; nét đứt: dự kiến.}
\label{fig:feedback_loop}
\end{figure}

Hình~\ref{fig:feedback_loop_en} là cùng vòng lặp đó, tiếng Anh và gọn hơn, \textbf{bỏ hẳn hai hộp
dự kiến}. Một sơ đồ dùng để trình bày cái đang có thì không nên dành một phần ba bề ngang cho cái
chưa có; bản đầy đủ ở Hình~\ref{fig:feedback_loop} vẫn giữ để đối chiếu.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/feedback_loop_en.pdf}
\caption{Bản rút gọn tiếng Anh. Mọi hộp trong hình đều đã cài đặt và có kiểm thử; màu cam là điểm
có người tham gia.}
\label{fig:feedback_loop_en}
\end{figure}

Bảng~\ref{tab:vi_sao_khong} giải thích vì sao không chọn các hướng cập nhật khác. Lý do chung là ở
vùng vài chục mẫu, replay có bằng chứng y tế mạnh nhất, còn các hướng còn lại hoặc cần nhiều mẫu
hơn, hoặc cần thành phần mà ứng dụng hiện không chạy.

\begin{table}[htbp]
\centering
\caption{Vì sao chọn head-only kèm replay thay cho các hướng cập nhật khác.}
\label{tab:vi_sao_khong}
\small
\begin{tabular}{@{}lp{9.2cm}@{}}
\toprule
\textbf{Hướng} & \textbf{Lý do không chọn} \\
\midrule
Head-only + replay + BN đóng băng & Đang dùng. Rẻ, bằng chứng y tế mạnh nhất ở vùng dữ liệu ít \\
EWC & Ma trận Fisher cần dữ liệu cũ; ở vài chục mẫu ước lượng không đáng tin. Thực nghiệm trên ảnh da liễu cho thấy replay thắng EWC ở mọi cấu hình \\
LwF & Bằng chứng y tế mỏng, không mạnh hơn replay \\
LoRA / adapter & Cần từ 50 mẫu trở lên; thêm phụ thuộc, thừa ở quy mô này \\
Tip-Adapter / prototype & Không thể quên và hoàn tác tức thì, nhưng cần BiomedCLIP, trong khi ứng dụng đang chạy nhánh CBAM \\
Huấn luyện lại toàn bộ & Quên thảm khốc khi số mẫu nhỏ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Cách đang dùng có tên gọi hay tự nghĩ ra?} Không có phần nào tự nghĩ ra. Đây là tổ hợp ba
thành phần đều đã chuẩn hoá trong y văn: \textbf{tinh chỉnh chỉ phần đầu} trên xương sống đóng băng
(linear probing), \textbf{bộ đệm replay} (rehearsal) trộn mẫu cũ vào mỗi lượt, và mô hình triển khai
\textbf{theo lô, ngoại tuyến, có người duyệt} đúng khung mà giới y tế dùng cho mô hình học liên tục.
Đóng góp nếu có không nằm ở thuật toán mà ở chỗ ráp ba thứ đó vào một ứng dụng chạy được, kèm tập
đánh giá đóng băng và cơ chế hoàn tác. Điều này là \emph{có chủ ý}: ở vùng vài chục mẫu, hướng đơn
giản nhất lại là hướng có bằng chứng y tế mạnh nhất, nên chọn nó thay vì phát minh là lựa chọn
phòng thủ được.

\textbf{Cơ chế từng hướng bị loại, và vì sao không hợp.}

\textit{EWC} (Kirkpatrick và cộng sự, PNAS 2017) sau khi học xong nhiệm vụ cũ sẽ ước lượng ma trận
thông tin Fisher --- đo mức nhạy của hàm mất mát theo từng trọng số --- rồi thêm một phạt bậc hai
kéo các trọng số \emph{quan trọng với nhiệm vụ cũ} ở lại gần giá trị cũ. Không hợp ở đây vì hai lẽ.
Ước lượng Fisher cần dữ liệu cũ và một siêu tham số cường độ phạt, trong khi đề tài không có ngân
sách dữ liệu để dò siêu tham số đó. Và quan trọng hơn: phạt chỉ bảo vệ những trọng số \emph{đang
được huấn luyện}, mà ở đây chỉ có 50 nghìn tham số đầu phân loại --- phần replay đã bảo vệ rồi.
Thực nghiệm trên ảnh da liễu cho thấy replay thắng EWC ở mọi cấu hình.

\textit{LwF} (Li và Hoiem, TPAMI 2018) giữ một bản sao mô hình cũ, rồi khi huấn luyện trên dữ liệu
mới thì buộc đầu ra của mô hình mới \emph{trên chính dữ liệu mới đó} bám theo đầu ra của mô hình cũ.
Ưu điểm là không cần giữ dữ liệu cũ. Nhưng đó cũng chính là lý do nó không hợp: \textbf{đề tài có
giữ dữ liệu cũ} (một cơ sở, tập huấn luyện nằm trong tay), nên lý do tồn tại của LwF không áp dụng.
Tệ hơn, dữ liệu mới ở đây \emph{là những ca mô hình cũ chấm sai} --- chưng cất theo đầu ra cũ trên
đúng những ca đó là dạy lại chính cái sai cần sửa.

\textit{LoRA và adapter} đóng băng xương sống rồi chèn các ma trận hạng thấp vào trong, huấn luyện
riêng phần chèn đó. Mục đích là \emph{thêm sức chứa} khi một đầu tuyến tính không đủ. Ở vài chục
mẫu thì vấn đề không phải thiếu sức chứa mà là thiếu dữ liệu: thêm sức chứa chỉ làm khớp thừa nhanh
hơn. Các báo cáo dùng được thường bắt đầu từ khoảng 50 mẫu trở lên, và nó kéo theo siêu tham số mới
(hạng, hệ số, chèn ở tầng nào) mà không có tập nào để dò.

\textit{Tip-Adapter và các hướng nguyên mẫu} dựng một bộ nhớ đệm gồm các cặp (đặc trưng, nhãn) từ
số mẫu ít ỏi, rồi lúc suy luận thì trộn điểm số zero-shot với một phiếu bầu có trọng số theo độ
tương đồng với bộ nhớ đó. Hướng này \textbf{rất hấp dẫn cho đúng bài toán này}: không cần huấn
luyện, và xoá một mục khỏi bộ nhớ là hiệu ứng biến mất tức thì --- tức quên và hoàn tác gần như
miễn phí. Lý do không dùng thuần tuý kỹ thuật: nó cần không gian nhúng ảnh--văn bản kiểu CLIP, tức
cần BiomedCLIP, trong khi đường suy luận của ứng dụng đang chạy nhánh CBAM. Dùng được thì phải phục
vụ một mô hình khác.

\textit{Huấn luyện lại toàn bộ} 63 triệu tham số trên vài chục mẫu trộn vào gần mười nghìn mẫu cũ
rơi vào một trong hai kết cục: hoặc gần như không đổi gì, hoặc nếu tăng trọng số cho mẫu mới thì
quên thảm khốc. Kèm theo là chi phí tính toán và gánh nặng kiểm chứng lại toàn bộ.

Bảng~\ref{tab:vi_sao_khong} bàn về \emph{cập nhật thế nào}. Còn một trục thứ hai đã khảo sát là
\emph{chọn ca nào cho bác sĩ xem trước}, tức active learning. Trục này hiện \textbf{chưa cài đặt},
nhưng các hướng đều đã có quyết định rõ ràng nên ghi lại ở Bảng~\ref{tab:active}.

\begin{table}[htbp]
\centering
\caption{Trục chọn mẫu (active learning): đã khảo sát, chưa cài đặt.}
\label{tab:active}
\small
\begin{tabular}{@{}lp{9.2cm}@{}}
\toprule
\textbf{Hướng} & \textbf{Kết luận} \\
\midrule
Lấy mẫu theo độ bất định & Rẻ nhất và được hậu thuẫn tốt nhất cho phân loại ảnh y tế (Gal, Islam và Ghahramani, ICML 2017). \textbf{Là hướng sẽ chọn} nếu cài trục này \\
Query-by-committee, coreset & Đẹp về học thuật nhưng quá tốn cho một GPU đơn ở quy mô mẫu nhỏ (Sener và Savarese, ICLR 2018). Loại \\
Rủi ro khởi đầu lạnh & Nhóm mẫu ban đầu nhỏ hoặc thiên lệch có thể làm active learning \textbf{tệ hơn chọn ngẫu nhiên} (COLosSAL, MICCAI 2023). Phải nêu như một hạn chế nếu cài \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Công cụ gần nhất đã có sẵn ngoài kia là MONAI Label} (Diaz-Pinto và cộng sự,
\textit{Medical Image Analysis}, 2024): cùng hạng mục với ứng dụng của đề tài, cũng tính độ bất
định để xếp thứ tự ca cho người đọc. Khác biệt của ứng dụng này nằm ở phần sau vòng lặp --- tập
đánh giá đóng băng, cổng macro-F1 và sổ đăng ký cho phép hoàn tác --- chứ không nằm ở phần chọn mẫu.

\subsection{Chuẩn bị dữ liệu từ chỉnh sửa}

Chỉnh sửa của bác sĩ không được dùng trực tiếp. Bước dựng dữ liệu làm bốn việc. Một, khử trùng lặp:
mỗi cặp (ca, ô nhãn) chỉ lấy lần sửa \textit{mới nhất}, vì bác sĩ có thể sửa đi sửa lại một ô. Hai,
chỉ lấy các lô đã gửi, và lấy được theo đúng một mã lô, để một lượt huấn luyện cũ tái lập được
chính xác những gì nó từng thấy. Ba, \textbf{cắt lại vùng ảnh} từ ảnh gốc và mặt nạ phân vùng bằng
đúng hàm mà lúc chấm đã dùng, cho ra khối $(9, 112, 224)$ --- không lưu ảnh tại thời điểm sửa, để
tránh lệch giữa lúc huấn luyện và lúc phục vụ. Bốn, ô nào bác sĩ không đụng tới thì gán $-1$, khớp
quy ước \textit{ignore\_index} mà nhánh huấn luyện chính đang dùng, nghĩa là không bịa nhãn cho
những đầu ra bác sĩ không nhận xét.

Ngoài dữ liệu chỉnh sửa, một lượt cập nhật còn cần hai tập nữa: tập replay khoảng 200 đĩa đệm gốc,
và tập đánh giá đã đóng băng. Cả hai lấy từ bộ RSNA 2024 đã tiền xử lý.

\textbf{Lượt cập nhật thứ hai trở đi lấy dữ liệu từ đâu.} Đây là một quyết định dễ bị bỏ qua nhưng
quyết định luôn việc cơ chế có trôi hay không. Kiến thức cũ tích luỹ ở \textit{trọng số}: mỗi lượt
nạp checkpoint đang phục vụ rồi tinh chỉnh tiếp, nên lượt thứ hai xuất phát từ kết quả của lượt
thứ nhất. Nhưng \textit{dữ liệu} thì không tự tích luỹ. Nếu mỗi lượt chỉ huấn luyện trên lô mới,
các chỉnh sửa của lô trước không còn xuất hiện trong tập huấn luyện, chúng chỉ còn tồn tại gián
tiếp trong trọng số --- đúng dạng trôi kinh điển của continual learning: chạy theo cái sửa mới
nhất và để những cái đã sửa trước đó phai dần. Tập replay không bù được, vì 200 đĩa đệm đó là dữ
liệu RSNA gốc, tức neo mô hình về \textit{phân phối ban đầu} chứ không neo về những gì bác sĩ đã
phán.

Do đó mặc định là \textbf{huấn luyện tích luỹ}: mỗi lượt dùng toàn bộ chỉnh sửa đã gửi từ trước
tới nay, để bản cập nhật nào cũng phải thoả mãn mọi điều bác sĩ từng sửa. Mã lô vẫn được giữ, nhưng
cho mục đích khác --- ghi sổ và tái lập chính xác một lượt chạy cũ --- và vẫn có thể chạy theo
đúng một lô khi cần đối chứng. Việc một ô bị sửa nhiều lần ở các lô khác nhau không gây trùng lặp,
vì bước dựng dữ liệu chỉ giữ bản sửa mới nhất của mỗi cặp (ca, ô nhãn).

Cách này có hai cái giá phải nói rõ. Thứ nhất, tập huấn luyện \textbf{phình dần} theo từng lượt;
tới một quy mô nào đó thì lập luận ``vài chục mẫu nên chỉ chỉnh ba đầu phân loại'' ở mục~3.2 không
còn đứng vững, và phải xét lại phương pháp cập nhật. Thứ hai, nó \textbf{buộc mọi chỉnh sửa cũ
phải được thoả mãn vô thời hạn}; nếu một bác sĩ đọc lệch thì cái lệch đó bị đóng đinh, và như
mục~3.6 đã nêu, chỉnh sửa của bác sĩ không phải chân lý --- mức đồng thuận giữa hai người đọc trên
thang Pfirrmann chỉ vào khoảng $\kappa = 0{,}68$. Hai giới hạn này thuộc phần cần theo dõi khi
vòng lặp thực sự chạy nhiều lượt.

\subsection{Giao thức đánh giá và cổng chặn}

Tập đánh giá được \textbf{chốt trước khi thu thập bất kỳ chỉnh sửa nào}: 200 dòng, đóng băng ngày
26/07/2026, kèm tệp kê khai ghi SHA-256 của các dòng đã chọn cùng hạt giống và thời điểm, và có
lệnh kiểm tra lại để chứng minh tập không đổi. Điều này bắt buộc chứ không phải cho chặt chẽ: nếu
chọn tập đánh giá \textit{sau} khi đã biết bác sĩ sửa những đĩa nào, con số đo được phản ánh cách
chọn chứ không phản ánh mô hình, và không có cách nào dựng lại bảo đảm đó về sau.

Chỉ tiêu quyết định là \textbf{macro-F1}, không phải độ chính xác. Lớp \textit{Severe} hiếm, nên một
mô hình sập về đoán \textit{Normal/Mild} cho mọi ô vẫn đạt độ chính xác trung bình cao trong khi mất
hết giá trị lâm sàng --- đúng lớp mà đề tài sinh ra để cải thiện. Trọng số mới chỉ được ghi ra tệp
khi macro-F1 sau lớn hơn trước; không đạt thì bỏ, và tệp trọng số giữ lại có nhúng sẵn số đo
trước/sau để người duyệt nhìn số mà quyết.

\subsection{Vì sao ``theo lô, ngoại tuyến, có người duyệt'' là thiết kế đúng}

Hướng dẫn của FDA tháng 12/2024 về \textit{Predetermined Change Control Plan} (PCCP) cho phần mềm
y tế dùng trí tuệ nhân tạo cho phép nhà sản xuất khai báo trước phạm vi thay đổi --- tần suất, giới
hạn dữ liệu, quy trình kiểm chứng, cách hoàn tác --- và sau đó cập nhật trong phạm vi đó mà không
phải xin duyệt lại từ đầu. Điểm mấu chốt là hướng dẫn này áp cho \textbf{cập nhật theo lô, ngoại
tuyến}, không áp cho học trực tuyến liên tục. Đạo luật AI của Liên minh châu Âu, hiệu lực
01/08/2024, cùng quy định về thiết bị y tế cũng đòi có giám sát của người và hậu kiểm.

Nghĩa là thiết kế ``gom lô, chạy ngoài giờ, người bấm duyệt, hoàn tác được'' không phải làm cho
đơn giản mà là làm cho đúng khung quản lý. Đây là lập luận nên nói thẳng khi bảo vệ, vì nó biến một
điểm trông như hạn chế thành một lựa chọn có căn cứ.

\subsection{Chỉnh sửa của bác sĩ không phải chân lý}

Một nghiên cứu ba người đọc trên ảnh MRI thắt lưng thoái hoá cho thấy chỉ số đồng thuận Cohen's
$\kappa$ với thang Pfirrmann chỉ đạt 0{,}68, tức hai bác sĩ bất đồng khoảng một phần ba số ca.
Do đó việc một bác sĩ sửa nhãn của mô hình không đương nhiên có nghĩa mô hình sai, và nuốt toàn bộ
chỉnh sửa vào tập huấn luyện có nguy cơ dạy mô hình thiên theo cách đọc của một người.

Hướng xử lý theo \textit{confident learning} (Northcutt, Jiang và Chuang, JAIR 2021) là đánh dấu
những chỉnh sửa mâu thuẫn với dự đoán mà mô hình đang rất tự tin để xem lại, thay vì đưa thẳng vào
huấn luyện. Lý tưởng là mỗi ca có ít nhất hai bác sĩ đọc độc lập; nếu không có thì phải ghi vào
phần hạn chế. Cơ chế truy vết cần thiết thì ứng dụng đã có sẵn: mỗi lần lưu tạo một phiên bản chú
giải mới và mỗi ô thay đổi được ghi một dòng nhật ký riêng.

\subsection{Đã làm tới đâu, và điều không được phép hứa}

Phần cơ chế đã cài đặt và có kiểm thử: dựng dữ liệu từ lô, huấn luyện lại đầu phân loại kèm ba chốt,
đóng băng tập đánh giá, cổng macro-F1, và sổ đăng ký mô hình cho phép kích hoạt lẫn hoàn tác. Các
chốt được kiểm thử theo hướng chứng minh \textit{hỏng thật khi bỏ chốt}, chẳng hạn có một kiểm thử
cho thấy thống kê BatchNorm trôi nếu không đóng băng.

Ba phần còn thiếu, nêu ra để không bị hiểu nhầm là đã xong. Một, giao thức thống kê mới có phần đo
trước/sau, \textbf{chưa có} kiểm định McNemar, khoảng tin cậy bootstrap cho $\Delta$F1, và chỉ số
backward transfer để đo quên (Lopez-Paz và Ranzato, NeurIPS 2017) --- tức hiện đủ sức \textit{chặn}
một bản cập nhật tệ nhưng chưa đủ sức \textit{khẳng định} một bản tốt. Hai, hàm mất mát đang dùng
entropy chéo trần, chưa có trọng số lớp, trong khi chính cấu hình này đã từng làm lớp \textit{Severe}
sập về F1 bằng 0 ở một thực nghiệm trước; cổng macro-F1 sẽ chặn hậu quả, nhưng nếu chạy mà không
cải thiện thì việc đầu tiên nên thử là thêm trọng số lớp. Ba, và đây là giới hạn đáng kể nhất về
phạm vi: \textbf{chỉ ba trong mười một nhãn có thể học được từ chỉnh sửa}. Bước dựng dữ liệu chỉ
ánh xạ ba đầu phân loại của mô hình CBAM; một chỉnh sửa trên tám nhãn còn lại --- vốn do nhánh
SpineNet gốc sinh ra --- vẫn được ghi nhật ký, vẫn vào lô, vẫn được đếm là ``đã gửi'', rồi bị bỏ
qua khi dựng tập huấn luyện. Nói cách khác, giao diện cho bác sĩ sửa nhiều hơn những gì vòng lặp
tiêu thụ được. Giao diện vận hành hiện đã đánh dấu các nhãn này, nhưng khoảng cách đó là có thật
và chỉ đóng lại được khi tám nhãn kia cũng được huấn luyện trong cùng một khung.

Quan trọng nhất: \textbf{vòng lặp chưa chạy trên chỉnh sửa thật của bác sĩ}, nên báo cáo này không
đưa ra con số trước/sau nào. Và ngay cả khi chạy, với quy mô 20--50 mẫu thì \textbf{không thể chứng
minh mô hình tốt lên một cách có ý nghĩa thống kê} --- các công trình continual learning trong ảnh
y tế dùng cỡ 100--1000 mẫu, còn vùng 20--50 chưa được kiểm chứng, và ngưỡng tối thiểu để một con số
macro-F1 bảo vệ được là khoảng 50--100 mẫu \textit{cho riêng lớp thiểu số}.

Vì vậy phát biểu mà phần này nhắm tới không phải ``phản hồi của bác sĩ làm mô hình tốt lên'' mà là
``cơ chế cập nhật an toàn, không gây quên, có cổng kiểm duyệt của người và có thể hoàn tác''. Khi
có số, câu kết luận đúng đắn sẽ có dạng: cơ chế không gây quên, có xu hướng cải thiện lớp đang yếu
với khoảng tin cậy kèm theo, và cỡ mẫu chưa đủ để kết luận có ý nghĩa thống kê.

\section{So sánh với phương pháp ngoài}

Hai phương pháp ngoài được huấn luyện lại trên \textbf{đúng cùng phép chia bệnh nhân}
(seed 42/123/456, chia 80/20 theo mã ca) và đánh giá trên cùng ba nhãn, để so sánh công bằng.

\begin{table}[htbp]
\centering\small
\caption{Các cấu hình đem so sánh.}
\label{tab:sota-what}
\begin{tabular}{lll}
\toprule
\textbf{Cấu hình} & \textbf{Cách xử lý chiều lát cắt} & \textbf{Nguồn} \\
\midrule
SpineNetV2      & tích chập 3D & nền so sánh của đề tài \\
CNN-BiLSTM & 2.5D: mã hoá từng lát, gộp bằng BiLSTM & theo giải nhì RSNA 2024 (\texttt{brendanartley}) \\
CNN-Transformer & 2.5D: mã hoá từng lát, gộp bằng tự chú ý & kiểu ViT, dựng làm đối chứng thứ hai \\
\textbf{Hybrid} & tích chập 3D + tiên nghiệm văn bản & của đề tài \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Hai phương pháp ngoài làm gì.} Cả hai đều theo lối \textbf{2.5D}: thay vì tích chập xuyên
qua chiều lát cắt, chúng mã hoá \emph{từng lát một} bằng một mạng 2D rồi mới gộp chín vectơ thu
được. Khác nhau ở cách gộp.

\textit{CNN-BiLSTM}, theo giải nhì RSNA 2024. Mỗi lát trong chín lát sagittal đi qua một bộ mã hoá ResNet18 riêng,
cho ra một chuỗi chín vectơ. Chuỗi này được đưa qua \textbf{BiLSTM} --- mạng hồi tiếp hai chiều,
đọc xuôi và đọc ngược để mỗi lát biết ngữ cảnh cả hai phía --- rồi \textbf{gộp có chú ý} thu chuỗi
về một vectơ duy nhất, với trọng số do chính mô hình học. Cuối cùng là ba đầu tuyến tính độc lập,
mỗi đầu ba lớp.

\textit{CNN-Transformer.} Cùng bộ mã hoá ResNet18 theo từng lát, nhưng chín vectơ được coi là
chín \emph{token}. Thêm một token \texttt{[CLS]} học được và nhúng vị trí, rồi cho cả mười token
qua một bộ mã hoá Transformer nhỏ; đầu ra tại vị trí \texttt{[CLS]} là biểu diễn của cả khối. Đây
là lối ViT quen thuộc, áp lên một chuỗi lát cắt ngắn thay vì các mảnh ảnh, và là lựa chọn
\emph{không hồi tiếp} đặt cạnh BiLSTM ở trên để hai đối chứng khác nhau về cơ chế chứ không chỉ
khác tên.

\textbf{Khác biệt cốt lõi so với đề tài} nằm ở hai chỗ. Một, cả hai chỉ nhìn xuyên lát \emph{sau
khi} đã mã hoá xong từng lát, trong khi mô hình của đề tài tích chập 3D nên thấy quan hệ giữa các
lát ngay từ tầng đầu. Hai, cả hai kết thúc bằng \textbf{đầu phân loại cố định}, nên không thể nhận
tập nhãn mới --- đó là nguồn gốc của Bảng~\ref{tab:sota-cap} bên dưới.

\begin{table}[htbp]
\centering\small
\caption{Kết quả trên RSNA 2024, trung bình $\pm$ độ lệch chuẩn qua 3 seed.}
\label{tab:sota}
\begin{tabular}{lcccc}
\toprule
\textbf{Chỉ số} & \textbf{SpineNetV2} & \textbf{CNN-BiLSTM} & \textbf{CNN-Transformer} & \textbf{Hybrid} \\
\midrule
Mean Accuracy (\%) & 81{,}0 $\pm$ 0{,}6 & \textbf{83{,}6 $\pm$ 0{,}5} & 83{,}4 $\pm$ 0{,}4 & \down{72{,}4 $\pm$ 0{,}3} \\
Mean F1 (macro)    & 0{,}420 $\pm$ 0{,}010 & \textbf{0{,}532 $\pm$ 0{,}008} & 0{,}527 $\pm$ 0{,}028 & 0{,}527 $\pm$ 0{,}027 \\
\midrule
\multicolumn{5}{l}{\textit{Lớp Severe --- lớp có ý nghĩa lâm sàng}} \\
Severe F1        & 0{,}152 $\pm$ 0{,}008 & 0{,}271 $\pm$ 0{,}026 & 0{,}257 $\pm$ 0{,}020 & \up{\textbf{0{,}356 $\pm$ 0{,}017}} \\
Severe Recall (\%) & 12{,}3 $\pm$ 0{,}6 & 26{,}0 $\pm$ 1{,}6 & 25{,}9 $\pm$ 2{,}8 & \up{\textbf{48{,}6 $\pm$ 1{,}9}} \\
\bottomrule
\end{tabular}
\end{table}

Bảng~\ref{tab:sota} cho thấy một sự đánh đổi rõ ràng. Hybrid \textbf{thua khoảng 11 điểm
Accuracy} và ngang bằng về Mean F1, nhưng ở lớp Severe thì dẫn đầu rõ rệt: Severe Recall
\textbf{48{,}6\%} so với 26{,}0\% và 25{,}9\%, tức \textbf{gần gấp đôi} phương pháp ngoài tốt nhất;
Severe F1 \textbf{0{,}356} so với 0{,}271.

Đánh đổi này là có chủ ý. Hai phương pháp ngoài đạt Accuracy cao bằng cách thiên về lớp đa số
(Normal/Mild chiếm phần lớn dữ liệu), trong khi ca bỏ sót ở lớp Severe mới là ca gây hậu quả lâm
sàng. Với bài toán sàng lọc, bỏ sót ba trong bốn ca nặng để đổi lấy Accuracy đẹp hơn là đánh đổi
sai hướng.

\textbf{Nhưng con số gộp che mất một điều đáng kể.} Bảng~\ref{tab:sota-f1} và
Bảng~\ref{tab:sota-recall} tách tiếp theo từng điều kiện và từng lớp. Ở lớp Severe:

\begin{table}[htbp]
\centering\small
\caption{Severe F1 theo từng điều kiện, trung bình $\pm$ độ lệch chuẩn qua 3 seed.}
\label{tab:sota-severe-cond}
\begin{tabular}{lcccc}
\toprule
\textbf{Điều kiện} & \textbf{SpineNetV2} & \textbf{CNN-BiLSTM} & \textbf{CNN-Transformer} & \textbf{Hybrid} \\
\midrule
Ống sống & 0{,}433 & \textbf{0{,}688} & 0{,}683 & \down{0{,}501} \\
Lỗ trái  & 0{,}022 & 0{,}088 & 0{,}059 & \up{\textbf{0{,}292}} \\
Lỗ phải  & \down{0{,}000} & 0{,}037 & 0{,}030 & \up{\textbf{0{,}273}} \\
\bottomrule
\end{tabular}
\end{table}

Đọc theo hàng thì bức tranh đảo chiều hai lần. \textbf{Ở ống sống, hai phương pháp ngoài thắng
Hybrid rõ rệt} --- 0{,}688 và 0{,}683 so với 0{,}501. Nhưng ở hai lỗ liên hợp thì chúng gần như
\textbf{không làm được gì}: Severe F1 rơi xuống 0{,}037 và 0{,}030 bên phải, trong khi Hybrid giữ
được 0{,}273. Riêng SpineNetV2 đạt đúng \textbf{0{,}000 ở cả ba seed} --- chưa một lần dự đoán
Severe cho lỗ phải.

Nghĩa là \textbf{toàn bộ lợi thế gộp của Hybrid đến từ hai nhãn lỗ liên hợp}, còn ở nhãn ống sống
thì nó đang thua. Nói thẳng điều này có lợi hơn là để con số gộp nói thay, vì nó khớp với phần còn
lại của báo cáo: lỗ liên hợp là chỗ \emph{mọi} mô hình đều gặp khó do ảnh đầu vào không chứa đủ
thông tin (mục~\ref{sec:t1}), và tiên nghiệm ngữ nghĩa đóng băng của Hybrid là thứ giữ nó không
sụp hẳn ở đó. Đổi lại, ở nhãn mà ảnh đã đủ thông tin thì một mô hình 2.5D chuyên biệt vẫn tốt hơn.

\begin{table}[htbp]\centering\small
\caption{F1 theo điều kiện $\times$ lớp, trung bình $\pm$ độ lệch chuẩn qua 3 seed.}
\label{tab:sota-f1}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}\toprule
\textbf{Điều kiện} & \textbf{Lớp} & \textbf{SpineNetV2} & \textbf{CNN-BiLSTM} & \textbf{CNN-Transformer} & \textbf{Hybrid} \\\midrule
Ống sống & Normal/Mild & 0{,}944\,$\pm$\,0{,}003 & \textbf{0{,}969\,$\pm$\,0{,}004} & 0{,}967\,$\pm$\,0{,}003 & 0{,}945\,$\pm$\,0{,}007 \\
 & Moderate & 0{,}119\,$\pm$\,0{,}011 & \textbf{0{,}476\,$\pm$\,0{,}021} & 0{,}469\,$\pm$\,0{,}073 & 0{,}323\,$\pm$\,0{,}159 \\
 & \textbf{Severe} & 0{,}433\,$\pm$\,0{,}016 & \textbf{0{,}688\,$\pm$\,0{,}025} & 0{,}683\,$\pm$\,0{,}028 & 0{,}501\,$\pm$\,0{,}064 \\
\midrule
Lỗ trái & Normal/Mild & 0{,}878\,$\pm$\,0{,}004 & 0{,}889\,$\pm$\,0{,}007 & \textbf{0{,}889\,$\pm$\,0{,}002} & 0{,}764\,$\pm$\,0{,}032 \\
 & Moderate & 0{,}252\,$\pm$\,0{,}062 & 0{,}372\,$\pm$\,0{,}086 & 0{,}402\,$\pm$\,0{,}098 & \textbf{0{,}421\,$\pm$\,0{,}042} \\
 & \textbf{Severe} & 0{,}022\,$\pm$\,0{,}039 & 0{,}088\,$\pm$\,0{,}031 & 0{,}059\,$\pm$\,0{,}051 & \textbf{0{,}292\,$\pm$\,0{,}017} \\
\midrule
Lỗ phải & Normal/Mild & 0{,}869\,$\pm$\,0{,}001 & \textbf{0{,}890\,$\pm$\,0{,}001} & 0{,}887\,$\pm$\,0{,}006 & 0{,}775\,$\pm$\,0{,}019 \\
 & Moderate & 0{,}264\,$\pm$\,0{,}015 & 0{,}380\,$\pm$\,0{,}056 & 0{,}360\,$\pm$\,0{,}138 & \textbf{0{,}443\,$\pm$\,0{,}010} \\
 & \textbf{Severe} & 0{,}000\,$\pm$\,0{,}000 & 0{,}037\,$\pm$\,0{,}023 & 0{,}030\,$\pm$\,0{,}033 & \textbf{0{,}273\,$\pm$\,0{,}022} \\
\bottomrule\end{tabular}\end{table}

\begin{table}[htbp]\centering\small
\caption{Recall theo điều kiện $\times$ lớp, trung bình $\pm$ độ lệch chuẩn qua 3 seed.}
\label{tab:sota-recall}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}\toprule
\textbf{Điều kiện} & \textbf{Lớp} & \textbf{SpineNetV2} & \textbf{CNN-BiLSTM} & \textbf{CNN-Transformer} & \textbf{Hybrid} \\\midrule
Ống sống & Normal/Mild & \textbf{0{,}986\,$\pm$\,0{,}000} & 0{,}979\,$\pm$\,0{,}004 & 0{,}971\,$\pm$\,0{,}002 & 0{,}937\,$\pm$\,0{,}012 \\
 & Moderate & 0{,}071\,$\pm$\,0{,}008 & 0{,}410\,$\pm$\,0{,}025 & \textbf{0{,}432\,$\pm$\,0{,}104} & 0{,}307\,$\pm$\,0{,}195 \\
 & \textbf{Severe} & 0{,}355\,$\pm$\,0{,}022 & 0{,}710\,$\pm$\,0{,}027 & \textbf{0{,}727\,$\pm$\,0{,}108} & 0{,}698\,$\pm$\,0{,}029 \\
\midrule
Lỗ trái & Normal/Mild & \textbf{0{,}963\,$\pm$\,0{,}012} & 0{,}945\,$\pm$\,0{,}027 & 0{,}936\,$\pm$\,0{,}021 & 0{,}650\,$\pm$\,0{,}044 \\
 & Moderate & 0{,}178\,$\pm$\,0{,}057 & 0{,}315\,$\pm$\,0{,}117 & 0{,}359\,$\pm$\,0{,}133 & \textbf{0{,}645\,$\pm$\,0{,}061} \\
 & \textbf{Severe} & 0{,}012\,$\pm$\,0{,}021 & 0{,}051\,$\pm$\,0{,}021 & 0{,}032\,$\pm$\,0{,}028 & \textbf{0{,}417\,$\pm$\,0{,}043} \\
\midrule
Lỗ phải & Normal/Mild & 0{,}948\,$\pm$\,0{,}006 & \textbf{0{,}956\,$\pm$\,0{,}017} & 0{,}951\,$\pm$\,0{,}025 & 0{,}668\,$\pm$\,0{,}036 \\
 & Moderate & 0{,}197\,$\pm$\,0{,}017 & 0{,}308\,$\pm$\,0{,}073 & 0{,}305\,$\pm$\,0{,}154 & \textbf{0{,}665\,$\pm$\,0{,}046} \\
 & \textbf{Severe} & 0{,}000\,$\pm$\,0{,}000 & 0{,}020\,$\pm$\,0{,}013 & 0{,}016\,$\pm$\,0{,}018 & \textbf{0{,}343\,$\pm$\,0{,}027} \\
\bottomrule\end{tabular}\end{table}



Ngoài chỉ số, Hybrid còn có ba khả năng mà cả ba phương pháp kia đều không có, do chúng dùng đầu
ra cố định:

\begin{table}[htbp]
\centering\small
\caption{So sánh khả năng mở rộng tập nhãn.}
\label{tab:sota-cap}
\begin{tabular}{lccc}
\toprule
\textbf{Khả năng} & \textbf{SpineNetV2} & \textbf{2 phương pháp ngoài} & \textbf{Hybrid} \\
\midrule
Nhận tập nhãn mới làm đầu vào      & không & không & \textbf{có} \\
Chuyển sang bộ dữ liệu khác (SPIDER) & phải huấn luyện lại & phải huấn luyện lại & \textbf{có} \\
Dự đoán nhãn chưa từng thấy         & không & không & \textbf{có} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Ghi chú về tính công bằng của phép so sánh.} Hai phương pháp ngoài là \textbf{cài đặt lại
phần lõi kiến trúc chấm điểm}, đã điều chỉnh để nhận cùng dạng vùng ảnh $(9,112,224)$ của đề tài
--- \textbf{không phải} pipeline đầu-cuối từ ảnh DICOM của tác giả gốc. Cả hai cũng huấn luyện từ
đầu vì không có trọng số tiền huấn luyện tương thích, trong khi Hybrid dùng backbone đã tiền
huấn luyện. Con số vì vậy phản ánh so sánh kiến trúc trên cùng biểu diễn dữ liệu, không phải so
sánh hai hệ thống hoàn chỉnh.

\section{Thực nghiệm Phase 3}

\subsection{Sửa lỗi khởi tạo: nền so sánh sạch}

Hai thực nghiệm trước đó dùng nhầm checkpoint khởi tạo (không có trọng số lớp). Chạy lại
đúng công thức đã công bố trên checkpoint đúng cho kết quả ở Bảng~\ref{tab:runc}.
Đây là \textbf{sửa lỗi, không phải đóng góp} --- giá trị của nó là mọi so sánh về sau không bị
bác bằng câu ``nền so sánh của em hỏng''.

\begin{table}[htbp]
\centering\small
\caption{Cùng công thức, khác checkpoint khởi tạo (seed 42, cùng tập validation).}
\label{tab:runc}
\begin{tabular}{lccl}
\toprule
\textbf{Chỉ số} & \textbf{Phase 2} & \textbf{Khởi tạo đúng} & \textbf{Chênh} \\
\midrule
Severe AUPRC    & 0{,}3211 & 0{,}3696 & \up{+0{,}0485} \\
Severe F1       & 0{,}3434 & 0{,}3696 & \up{+0{,}0262} \\
Mean Accuracy   & 0{,}7208 & 0{,}7318 & \up{+0{,}0110} \\
Mean F1 (macro) & 0{,}5277 & 0{,}5286 & \up{+0{,}0009} \\
Macro AUPRC     & 0{,}5258 & 0{,}5364 & \up{+0{,}0106} \\
Macro AUC       & 0{,}8371 & 0{,}8368 & $-$0{,}0003 \\
\bottomrule
\end{tabular}
\end{table}

Bảng~\ref{tab:percond} tách theo từng nhãn, và đây mới là chỗ giải thích vì sao phải làm
bước tiếp theo.

\begin{table}[htbp]
\centering\small
\caption{Kết quả từng nhãn, Phase 2 so với khởi tạo đúng (seed 42, cùng tập validation).}
\label{tab:percond}
\begin{tabular}{lccc}
\toprule
 & \textbf{Phase 2} & \textbf{Khởi tạo đúng} & \textbf{Chênh} \\
\midrule
\multicolumn{4}{l}{\textit{Ống sống}} \\
\quad Severe F1      & 0{,}5000 & 0{,}5714 & \up{+0{,}0714} \\
\quad Severe Recall  & 0{,}7037 & 0{,}8395 & \up{+0{,}1358} \\
\quad AUPRC          & 0{,}6284 & 0{,}6506 & \up{+0{,}0222} \\
\midrule
\multicolumn{4}{l}{\textit{Lỗ liên hợp trái}} \\
\quad AUPRC          & 0{,}4670 & 0{,}4709 & +0{,}0039 \\
\quad Severe F1      & 0{,}2778 & 0{,}2887 & +0{,}0109 \\
\midrule
\multicolumn{4}{l}{\textit{Lỗ liên hợp phải}} \\
\quad AUPRC          & 0{,}4820 & 0{,}4878 & +0{,}0058 \\
\quad Severe F1      & 0{,}2524 & 0{,}2486 & \down{$-$0{,}0038} \\
\bottomrule
\end{tabular}
\end{table}

Ống sống cải thiện rõ (Severe Recall \up{+0{,}136}), nhưng \textbf{hai nhãn lỗ liên hợp gần như
đứng yên} --- AUPRC chỉ nhích 0{,}004 và 0{,}006. Sửa khởi tạo không chạm được tới điểm yếu này,
nghĩa là nguyên nhân nằm ở chỗ khác. Đó là điều dẫn tới phần tiếp theo.

\subsection{Chuyên gia T1 cho nhãn lỗ liên hợp: hướng chính, chưa kết luận}
\label{sec:t1}

\textbf{Chẩn đoán.} Đối chiếu tệp toạ độ với mô tả chuỗi ảnh: nhãn lỗ liên hợp được chấm
\textbf{100\% trên Sagittal T1}, nhãn ống sống \textbf{99{,}9\% trên Sagittal T2}, và hai loại
chỉ dùng chung một chuỗi \textbf{1 lần trên 6291} ca. Quy trình cũ cắt một vùng ảnh duy nhất từ
T2 lấy tâm theo toạ độ ống sống rồi gắn cả ba nhãn.

Đo độ lệch trong \textbf{không gian bệnh nhân} của DICOM (dùng \texttt{ImagePositionPatient} và
\texttt{ImageOrientationPatient}, không so toạ độ điểm ảnh vì hai chuỗi có lưới khác nhau):

\begin{table}[htbp]
\centering\small
\caption{Vị trí nhãn lỗ liên hợp so với vùng ảnh cắt từ T2 (796 cặp, 80 ca).}
\label{tab:geo}
\begin{tabular}{lcc}
\toprule
 & \textbf{Lỗ trái} & \textbf{Lỗ phải} \\
\midrule
Lệch trong mặt phẳng (trung vị)      & 9{,}7 mm & 9{,}9 mm \\
Nằm ngoài khung ảnh theo mặt phẳng   & 0{,}3\% & 0{,}3\% \\
Lệch theo chiều sâu (trung vị)       & 4{,}0 lát & 4{,}0 lát \\
\textbf{Nằm ngoài độ sâu 9 lát}      & \down{\textbf{32{,}2\%}} & \down{\textbf{47{,}2\%}} \\
\bottomrule
\end{tabular}
\end{table}

Khung ảnh theo mặt phẳng rất rộng (nửa cạnh 70$\times$35 mm) nên hầu như luôn phủ được. Chỗ hụt
là \textbf{độ sâu}: vùng ảnh chỉ gồm 9 lát, trong khi gần một nửa số ca bên phải có vùng cần chấm
nằm ngoài phạm vi đó. Nói cách khác, mô hình được yêu cầu chấm một cấu trúc \textbf{không có trong
ảnh đầu vào}.

\textbf{Kết quả sau khi sửa.} Cắt vùng ảnh riêng cho từng bên trên chuỗi T1, lấy tâm theo toạ độ
của chính bên đó:

\begin{table}[htbp]
\centering\small
\caption{AUPRC nhãn lỗ liên hợp. Cột ``đỉnh'' là epoch tốt nhất trong 20 epoch.}
\label{tab:t1}
\begin{tabular}{lcccc}
\toprule
 & \textbf{Phase 2} & \textbf{Chuyên gia T1} & \textbf{Chênh} & \textbf{Đỉnh ep16} \\
\midrule
\multicolumn{5}{l}{\textit{AUPRC --- chỉ số xếp hạng, không phụ thuộc ngưỡng}} \\
\quad Lỗ trái  & 0{,}4670 & 0{,}5205 & \up{+0{,}054} & 0{,}5321 \\
\quad Lỗ phải  & 0{,}4820 & 0{,}5209 & \up{+0{,}039} & 0{,}5311 \\
\midrule
\multicolumn{5}{l}{\textit{AUC}} \\
\quad Lỗ trái  & 0{,}7880 & 0{,}8165 & \up{+0{,}029} & 0{,}8281 \\
\quad Lỗ phải  & 0{,}7964 & 0{,}8147 & \up{+0{,}018} & 0{,}8268 \\
\midrule
\multicolumn{5}{l}{\textit{Severe F1 --- phụ thuộc ngưỡng argmax}} \\
\quad Lỗ trái  & 0{,}2778 & 0{,}2247 & \down{$-$0{,}053} & --- \\
\quad Lỗ phải  & 0{,}2524 & 0{,}2902 & \up{+0{,}038} & --- \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Severe F1 lỗ trái lại giảm, và điều này cần giải thích chứ không nên bỏ qua.} AUPRC tăng
trong khi F1 giảm nghĩa là mô hình \textbf{xếp hạng ca nặng tốt hơn nhưng điểm cắt argmax chưa
khai thác được lợi thế đó}. Đây đúng là việc mà tinh chỉnh ngưỡng giải quyết: đo bằng kiểm chéo
theo nửa bệnh nhân, chỉnh trọng số quyết định từng lớp nâng macro F1 từ 0{,}468 lên
\textbf{0{,}509} (\up{+0{,}041}) trên phần dữ liệu giữ lại. Trên mô hình Phase 2 cùng phương pháp
cho \up{+0{,}019}. Hai cải thiện này cộng dồn được vì chúng tác động vào hai khâu khác nhau ---
một cái cải thiện biểu diễn, một cái cải thiện quy tắc quyết định.

Điều đáng tin hơn con số: \textbf{cả 20/20 epoch} của mô hình T1 đều vượt epoch tốt nhất trong
20 epoch của mô hình cũ, ở cả hai bên, không ngoại lệ. Tính nhất quán này loại trừ khả năng
vớ được một epoch may.

\textbf{Ghép hai nhánh thành một hệ thống, và con số trên phải đọc lại.} Hệ mà luận văn đề xuất
không phải từng nhánh riêng mà là hai nhánh định tuyến theo điều kiện. Ghép chúng bằng cách nối dự
đoán theo khoá (ca, tầng), không huấn luyện lại. Phép ghép này lập tức chạm vào lỗi chia tập nêu ở
mục sau: hai tập validation chỉ chung \textbf{214 trên 395} bệnh nhân, nên chỉ chấm được trên phần
giao đó --- 181 bệnh nhân còn lại nằm trong tập huấn luyện của một trong hai nhánh.

Hệ quả là hai con số $+0{,}054$ và $+0{,}039$ ở Bảng~\ref{tab:t1} \textbf{không so trên cùng một
nhóm bệnh nhân}. Khi so đúng cặp, trên cùng các dòng dữ liệu:

\begin{table}[htbp]
\centering\small
\caption{AUPRC macro nhãn lỗ liên hợp, so trên 214 bệnh nhân cả hai nhánh đều giữ lại.}
\label{tab:matched}
\begin{tabular}{lccc}
\toprule
 & \textbf{Nhánh T2} & \textbf{Nhánh T1} & \textbf{Chênh} \\
\midrule
Lỗ trái  & 0{,}4767 & 0{,}5121 & \up{+0{,}035} \\
Lỗ phải  & 0{,}5171 & 0{,}5099 & \down{$-$0{,}007} \\
\bottomrule
\end{tabular}
\end{table}

Bên trái vẫn dương nhưng nhỏ hơn con số đã báo; \textbf{bên phải mất hẳn}. Toàn hệ thống, Mean F1
macro là 0{,}5311 (chỉ T2) so với 0{,}5108 (có định tuyến), tức $-0{,}020$ với khoảng tin cậy 95\%
$[-0{,}046; +0{,}005]$ và $p = 0{,}122$: định tuyến \textbf{không cho thấy cải thiện}, mà cũng chưa
đủ bằng chứng nói nó làm tệ đi.

Cả hai phép so đều nhiễu --- 214 bệnh nhân, một seed, mỗi điều kiện 40--48 ca Severe. Khác biệt là
phép so này \textbf{hợp lệ}, còn phép so cũ bị nhiễu bởi việc bệnh nhân nào rơi vào tập nào. Vì
vậy phát biểu đúng lúc này là: \textbf{giả thuyết định tuyến chưa được chứng minh}, và việc quyết
định nó là chạy lại cả hai nhánh trên cùng một phép chia theo hàm băm. Mã tái lập ở
\texttt{experiments/routing/assemble\_routed\_system.py}.

\textbf{Kiểm chứng nguyên nhân.} Nếu vùng ảnh thật sự không thấy được lỗ liên hợp thì mô hình cũ
phải kém hơn ở đúng những ca đó. Kiểm trong nội bộ một mô hình, chia theo một thuộc tính của dữ
liệu mà mô hình không biết:

\begin{table}[htbp]
\centering\small
\caption{AUC lớp Severe của mô hình T2, chia theo vị trí nhãn.}
\label{tab:causal}
\begin{tabular}{lcccc}
\toprule
 & \textbf{Trong 9 lát} & \textbf{Ngoài 9 lát} & \textbf{Chênh} & \textbf{p} \\
\midrule
Lỗ phải & 0{,}8983 & 0{,}8321 & \up{+0{,}066} & \textbf{0{,}044} \\
Lỗ trái & 0{,}8685 & 0{,}8643 & +0{,}004 & 0{,}903 \\
\bottomrule
\end{tabular}
\end{table}

Bên phải xác nhận, bên trái không. Hai bên khác nhau theo đúng hướng dự đoán: bên phải có
47{,}2\% ca ngoài phạm vi so với 32{,}2\% của bên trái, tức nhiều ca bị ảnh hưởng hơn thì hiệu ứng
lớn hơn. \textbf{Giới hạn cần nêu:} đây là hai phép kiểm, một cái đạt $p=0{,}044$; hiệu chỉnh cho
việc so nhiều lần thì $p=0{,}088$, nên kết luận đúng là \textbf{có dấu hiệu ủng hộ}, chưa phải
bằng chứng chắc chắn.

\subsection{Bốn hướng đã thử và không hiệu quả}

Các kết quả âm dưới đây đều đáng báo cáo vì chúng loại bỏ những hướng nghe hợp lý.

\begin{table}[htbp]
\centering\small
\caption{Các hướng cải thiện đã thử nghiệm và kết quả.}
\label{tab:neg}
\begin{tabular}{p{4.6cm}p{9.8cm}}
\toprule
\textbf{Hướng} & \textbf{Kết quả} \\
\midrule
Cơ chế trộn có cổng (gated fusion) &
Chênh lệch đỉnh 0{,}0016, nhỏ hơn độ lệch chuẩn giữa các epoch (0{,}0048). \textbf{Không khác biệt.} \\
Trung bình trọng số nhiều epoch &
Kém hơn epoch đơn tốt nhất ở mọi chỉ số. \textbf{Không có tác dụng.} \\
Mở băng tầng cuối của backbone &
Quá khớp: Mean Accuracy \up{+0{,}029} nhưng Severe AUPRC \down{$-$0{,}037}. Quá khớp ăn vào lớp hiếm trước. \\
Bản đồ nhiệt (Grad-CAM) để định vị &
Thua phép đoán ``giữa ảnh'': 30{,}7 mm so với 21{,}6 mm. \textbf{Không định vị được.} \\
\bottomrule
\end{tabular}
\end{table}

Kết quả mở băng backbone trở thành \textbf{lý lẽ bảo vệ} thiết kế đóng băng hiện tại, khi được hỏi
``tại sao không tinh chỉnh toàn bộ mạng''.

\section{Khả diễn giải: khảo sát và kiểm chứng}

Theo yêu cầu khảo sát nhiều hướng tiếp cận, đã rà 16 phương pháp thuộc bốn họ: CAM dùng gradient,
CAM không gradient, quy gán ở không gian đầu vào, và tín hiệu nội tại của mô hình. Chọn triển khai
bốn phương pháp có \textbf{cơ chế khác nhau} thay vì nhiều biến thể cùng một cơ chế.

Hai kết quả kiểm chứng đáng lưu:

\textbf{Grad-CAM trên nhánh CBAM là phân rã chính xác, không phải suy đoán.} Đầu ra của mạng là
gộp trung bình toàn cục rồi một tầng tuyến tính --- đúng kiến trúc mà Draelos \& Carin chứng minh
CAM, HiResCAM và Grad-CAM cho cùng một kết quả. Kiểm bằng số trên mô hình của đề tài: tương quan
Pearson \textbf{1{,}000000}, sai lệch tuyệt đối lớn nhất $2{,}4\times10^{-7}$.

\textbf{Bản đồ nhiệt không định vị được lỗ liên hợp.} Đo khoảng cách từ đỉnh bản đồ nhiệt tới điểm
bác sĩ chấm, kèm hai mốc đối chứng bắt buộc:

\begin{table}[htbp]
\centering\small
\caption{Khoảng cách tới điểm bác sĩ chấm (trung vị, mm), các ca Severe.}
\label{tab:loc}
\begin{tabular}{lcc}
\toprule
 & \textbf{Lỗ trái (n=53)} & \textbf{Lỗ phải (n=20)} \\
\midrule
Grad-CAM              & 30{,}7 & 33{,}9 \\
Layer-CAM             & 31{,}8 & 30{,}4 \\
Đoán ngẫu nhiên       & 49{,}7 & 53{,}7 \\
\textbf{Đoán giữa ảnh} & \textbf{21{,}6} & \textbf{19{,}6} \\
\bottomrule
\end{tabular}
\end{table}

Cả hai phương pháp thắng phép đoán ngẫu nhiên nhưng \textbf{thua phép đoán ``giữa ảnh''}, nên chúng
không mang thông tin định vị hữu ích. Kết quả này trùng với Arun và cộng sự (\textit{Radiology: AI},
2021): cả 8 phương pháp họ thử trên ảnh y tế thật đều trượt ít nhất một tiêu chí tin cậy.

Hệ quả cần ghi nhận: hình Grad-CAM dùng trong bài báo đã nộp \textbf{không chứng minh được} điều nó
tuyên bố. Vùng ống sống nằm ngay giữa ảnh do cách cắt, nên ``mô hình tập trung vào ống sống'' không
phân biệt được với ``mô hình tập trung vào giữa ảnh'' --- mốc đối chứng cho 0{,}4 mm trong khi
Grad-CAM cho 14{,}0 mm.

\section{Hội nghị MIWAI 2026: tài liệu đã chuẩn bị}

Bài báo \textit{A Two-Branch Hybrid Model for Imbalanced Lumbar Disc Grading and Zero-Shot Label
Extension} (paper 122) đã được chấp nhận báo cáo miệng. Ban tổ chức yêu cầu \textbf{mọi bài} tham
gia phiên bất đồng bộ, gồm ba món: phần tóm tắt, một trang infographic, và một video.

\begin{table}[htbp]
\centering\small
\caption{Tài liệu hội nghị, trạng thái tại thời điểm báo cáo.}
\label{tab:miwai}
\begin{tabular}{@{}lp{9.4cm}@{}}
\toprule
\textbf{Tài liệu} & \textbf{Trạng thái} \\
\midrule
Tóm tắt & Xong, 247 từ, lấy nguyên văn từ bài đã chấp nhận \\
Infographic & Xong, một trang 1920$\times$1080. Mọi số trên trang truy được về bảng trong bài báo \\
Video & \down{Chưa quay.} Slide và kịch bản nói là nguyên liệu \\
Slide báo cáo & Xong, 23 trang: 14 trang trình bày, 3 trang tài liệu tham khảo, 5 trang dự phòng \\
Kịch bản nói & Xong, bấm giờ từng slide, chạy 11--13 phút trong khung 15 phút \\
\bottomrule
\end{tabular}
\end{table}

Slide và kịch bản \textbf{không phải món nộp} --- chúng là nguyên liệu để quay video và để trình
bày trực tuyến. Kịch bản kèm phần hỏi đáp đã soạn sẵn 11 câu, trong đó có hai câu khó nhất: vì sao
độ chính xác tổng giảm, và vì sao trung bình tám nhãn zero-shot lại thua mô hình chưa huấn luyện.

Toàn bộ nằm ở \texttt{paper/camera\_ready/miwai\_async/}, đóng gói sẵn một tệp nén để gửi.

\section{Số liệu chi tiết các phase trước}

Mục này là phần tra cứu, không nằm trong mạch trình bày. Gom lại để khi cần một con số cụ thể thì
mở đúng một chỗ, thay vì lật ba tệp.

\subsection{Bốn cấu hình nội bộ}

Bảng~\ref{tab:phase2} là bốn cấu hình nội bộ, Bảng~\ref{tab:phase2full} tách tiếp theo từng điều
kiện và từng mức độ. Mọi ô ở cả hai bảng đều \textbf{tính lại từ tệp số liệu lưu kèm mỗi lần chạy}
trong \texttt{checkpoints/v3\_20260503/}, nên truy ngược được về đúng lần chạy đã sinh ra chúng.

\textbf{Ba ô trong bản trước bị sai và đã sửa ở đây.} Hàng Severe Recall ghi 0{,}370 cho CBAM và
0{,}245 cho BMC-only, trong khi tệp số liệu cho 0{,}362 và \textbf{0{,}313}; sai lệch ở BMC-only
là 6{,}8 điểm và làm cấu hình đó bị đánh giá thấp hơn thực tế. Hàng Mean F1 macro ghi 0{,}420 cho
SpineNetV2 trong khi tệp cho 0{,}429 --- 0{,}420 là con số 3 seed của bài báo, lọt vào một bảng
vốn ghi rõ là seed 42. Các hàng Severe F1, Severe AUPRC và Mean AUC khớp chính xác.

\begin{table}[htbp]\centering\small
\caption{Bốn cấu hình trên RSNA 2024 (seed 42), mọi ô tính lại từ tệp số liệu lưu kèm mỗi lần chạy.}
\label{tab:phase2}
\begin{tabular}{lcccc}\toprule
\textbf{Chỉ số} & \textbf{SpineNetV2} & \textbf{CBAM} & \textbf{BMC-only} & \textbf{Hybrid} \\\midrule
Mean Accuracy & \textbf{0{,}813} & 0{,}690 & 0{,}692 & 0{,}721 \\
Mean Recall & 0{,}417 & 0{,}569 & 0{,}528 & \textbf{0{,}591} \\
Mean Precision & 0{,}492 & 0{,}510 & 0{,}475 & \textbf{0{,}517} \\
Mean F1 macro & 0{,}429 & 0{,}502 & 0{,}463 & \textbf{0{,}528} \\
Macro AUC & 0{,}826 & 0{,}822 & 0{,}812 & \textbf{0{,}837} \\
Macro AUPRC & 0{,}523 & 0{,}519 & 0{,}482 & \textbf{0{,}526} \\
\midrule\multicolumn{5}{l}{\textit{Lớp Severe}} \\
Severe Recall & 0{,}115 & 0{,}362 & 0{,}313 & \textbf{0{,}464} \\
Severe Precision & 0{,}212 & 0{,}272 & 0{,}211 & \textbf{0{,}273} \\
Severe F1 & 0{,}149 & 0{,}304 & 0{,}229 & \textbf{0{,}343} \\
Severe AUC & 0{,}860 & 0{,}886 & 0{,}874 & \textbf{0{,}896} \\
Severe AUPRC & 0{,}276 & 0{,}319 & 0{,}218 & \textbf{0{,}321} \\
\bottomrule\end{tabular}\end{table}

\begin{table}[htbp]\centering\scriptsize
\caption{Chi tiết theo điều kiện $\times$ mức độ cho cả bốn cấu hình (seed 42). Mỗi ô: Recall / Precision / F1.}
\label{tab:phase2full}
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{llcccc}\toprule
\textbf{Điều kiện} & \textbf{Mức độ} & \textbf{SpineNetV2} & \textbf{CBAM} & \textbf{BMC-only} & \textbf{Hybrid} \\\midrule
Ống sống & Normal/Mild & 0{,}987 / 0{,}909 / 0{,}946 & 0{,}921 / 0{,}975 / 0{,}947 & 0{,}892 / 0{,}950 / 0{,}920 & \textbf{0{,}933 / 0{,}972 / 0{,}952} \\
 & Moderate & 0{,}079 / 0{,}379 / 0{,}131 & 0{,}446 / 0{,}339 / 0{,}385 & 0{,}201 / 0{,}226 / 0{,}213 & \textbf{0{,}396 / 0{,}385 / 0{,}390} \\
 & \textbf{Severe} & 0{,}346 / 0{,}636 / 0{,}448 & \textbf{0{,}716 / 0{,}436 / 0{,}542} & 0{,}691 / 0{,}277 / 0{,}396 & 0{,}704 / 0{,}388 / 0{,}500 \\
\midrule
Lỗ trái & Normal/Mild & \textbf{0{,}950 / 0{,}812 / 0{,}875} & 0{,}619 / 0{,}920 / 0{,}740 & 0{,}609 / 0{,}933 / 0{,}737 & 0{,}599 / 0{,}926 / 0{,}727 \\
 & Moderate & 0{,}242 / 0{,}470 / 0{,}319 & 0{,}683 / 0{,}294 / 0{,}411 & \textbf{0{,}753 / 0{,}301 / 0{,}430} & 0{,}675 / 0{,}292 / 0{,}408 \\
 & \textbf{Severe} & 0{,}000 / 0{,}000 / 0{,}000 & 0{,}212 / 0{,}183 / 0{,}197 & 0{,}150 / 0{,}203 / 0{,}173 & \textbf{0{,}375 / 0{,}221 / 0{,}278} \\
\midrule
Lỗ phải & Normal/Mild & \textbf{0{,}951 / 0{,}800 / 0{,}869} & 0{,}538 / 0{,}952 / 0{,}687 & 0{,}627 / 0{,}919 / 0{,}745 & 0{,}707 / 0{,}912 / 0{,}797 \\
 & Moderate & 0{,}199 / 0{,}423 / 0{,}271 & 0{,}828 / 0{,}298 / 0{,}438 & 0{,}734 / 0{,}312 / 0{,}438 & \textbf{0{,}621 / 0{,}347 / 0{,}446} \\
 & \textbf{Severe} & 0{,}000 / 0{,}000 / 0{,}000 & 0{,}157 / 0{,}197 / 0{,}174 & 0{,}096 / 0{,}154 / 0{,}119 & \textbf{0{,}313 / 0{,}211 / 0{,}252} \\
\bottomrule\end{tabular}\end{table}

\subsection{Chi tiết theo từng bệnh và từng mức độ}

Bảng~\ref{tab:phase2full} ở trên đã tách Recall, Precision và F1 theo điều kiện $\times$ lớp cho cả
bốn cấu hình. Bảng~\ref{tab:phase2auc} bổ sung hai chỉ số \textbf{không phụ thuộc ngưỡng} với cùng
cách chia, để phân biệt phần cải thiện đến từ biểu diễn đặc trưng với phần đến từ việc dịch điểm
cắt quyết định.

\begin{table}[htbp]\centering\scriptsize
\caption{AUC / AUPRC theo điều kiện $\times$ lớp cho cả bốn cấu hình (seed 42). Mỗi ô: AUC / AUPRC.}
\label{tab:phase2auc}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}\toprule
\textbf{Điều kiện} & \textbf{Lớp} & \textbf{SpineNetV2} & \textbf{CBAM} & \textbf{BMC-only} & \textbf{Hybrid} \\\midrule
Ống sống & Normal/Mild & 0{,}892 / 0{,}984 & \textbf{0{,}947 / 0{,}993} & 0{,}874 / 0{,}981 & 0{,}946 / 0{,}993 \\
 & Moderate & 0{,}851 / 0{,}272 & \textbf{0{,}884 / 0{,}319} & 0{,}783 / 0{,}203 & 0{,}872 / 0{,}299 \\
 & \textbf{Severe} & 0{,}922 / 0{,}513 & \textbf{0{,}963 / 0{,}602} & 0{,}907 / 0{,}331 & 0{,}962 / 0{,}594 \\
\midrule
Lỗ trái & Normal/Mild & 0{,}794 / 0{,}932 & 0{,}789 / 0{,}924 & 0{,}803 / 0{,}936 & \textbf{0{,}815 / 0{,}938} \\
 & Moderate & \textbf{0{,}758 / 0{,}387} & 0{,}655 / 0{,}262 & 0{,}710 / 0{,}306 & 0{,}680 / 0{,}289 \\
 & \textbf{Severe} & 0{,}815 / 0{,}147 & 0{,}858 / 0{,}174 & 0{,}854 / 0{,}169 & \textbf{0{,}869 / 0{,}174} \\
\midrule
Lỗ phải & Normal/Mild & 0{,}802 / 0{,}929 & 0{,}807 / 0{,}931 & 0{,}808 / 0{,}934 & \textbf{0{,}823 / 0{,}939} \\
 & Moderate & \textbf{0{,}760 / 0{,}375} & 0{,}657 / 0{,}283 & 0{,}705 / 0{,}325 & 0{,}708 / 0{,}312 \\
 & \textbf{Severe} & 0{,}842 / 0{,}167 & 0{,}837 / 0{,}181 & \textbf{0{,}861 / 0{,}153} & 0{,}858 / 0{,}195 \\
\bottomrule\end{tabular}\end{table}

Lớp Severe tăng cả AUC lẫn AUPRC trên cả ba điều kiện khi đi từ SpineNetV2 sang Hybrid. Vì hai chỉ
số này đánh giá thứ tự xếp hạng, kết quả loại trừ khả năng cải thiện chỉ là hệ quả của việc dịch
ngưỡng.

\textbf{Một chênh lệch với bảng trong luận văn, ghi lại để khỏi lẫn.} Chương 5 luận văn ghi Severe
Precision của Hybrid ở nhãn ống sống là 0{,}559, dẫn tới F1 0{,}623. Tệp số liệu lưu kèm lần chạy
cho \textbf{0{,}388} và F1 \textbf{0{,}500}; riêng Recall thì khớp chính xác ở 0{,}704. Báo cáo này
dùng số trong tệp vì truy ngược được về đúng lần chạy. Cần rà lại chương 5 trước khi nộp luận văn.

\section{Ba lỗi phương pháp đã phát hiện}

\textbf{Sai hệ toạ độ.} Phép đo hình học ban đầu trừ toạ độ điểm ảnh của hai chuỗi T1 và T2 như thể
chúng cùng một hệ. Ba trong bốn ca kiểm tra có lưới ảnh khác nhau (384$\times$384 ở 0{,}78 mm so với
640$\times$640 ở 0{,}47 mm). Tính lại trong không gian bệnh nhân thì lập luận vẫn đứng nhưng
\textbf{đổi trục}: không phải ``ngoài khung hình'' mà là ``ngoài độ sâu 9 lát''.

\textbf{Sai phép chia tập bệnh nhân.} Hàm chia chạy trên danh sách bệnh nhân của từng tệp mô tả dữ
liệu; hai tệp lệch nhau 3 ca nên sinh ra hai phép chia hoàn toàn khác nhau. Hai tập validation chỉ
chung 214 trên 395 bệnh nhân, và 181 bệnh nhân nằm ở tập kiểm của mô hình này lại nằm ở tập huấn
luyện của mô hình kia. Việc này \textbf{không làm sai} số của từng mô hình, nhưng làm phép
\textbf{so sánh} giữa chúng lỏng hơn mức đã trình bày. Đã sửa bằng cách chia theo hàm băm mã bệnh
nhân, cho 406 bệnh nhân trùng khớp 100\% giữa hai tập dữ liệu.

\textbf{Chép nhầm ô trong bảng tóm tắt luận văn.} Bảng tổng hợp bốn cấu hình ở chương 5 ghi Mean
F1 macro là 0{,}343 $\to$ 0{,}474, và phần tóm tắt nhắc lại thành mức cải thiện $+0{,}131$. Tính
lại chỉ số này từ bảng per-class nằm ngay cùng trang (F1 từng lớp 0{,}898 / 0{,}211 / 0{,}152 và
0{,}786 / 0{,}406 / 0{,}333) cho \textbf{0{,}4203 $\to$ 0{,}5083}. Giá trị nền tính lại khớp bốn
chữ số thập phân với số 3 seed của bài báo (0{,}4202), nên 0{,}343 là sai --- đó là Severe F1 của
Hybrid, nằm cách hai dòng trong cùng bảng. Mức cải thiện thật là $+0{,}088$ (một seed) hoặc
$+0{,}106$ (ba seed). Báo cáo này không bị ảnh hưởng vì đã dùng 0{,}420 $\to$ 0{,}528.

\section{Kế hoạch tiếp theo}

\begin{enumerate}
\itemsep0.15em
\item \textbf{Chạy lại hai nhánh trên cùng một phép chia} bằng chế độ băm mã bệnh nhân, rồi so
trên đủ 395 bệnh nhân. Đây là việc \emph{bắt buộc}, không phải tuỳ chọn: phát biểu ``định tuyến T1
cải thiện nhãn lỗ liên hợp'' hiện đứng trên một phép so không hợp lệ, và ghép hệ thống trên phần
giao 214 bệnh nhân không cho thấy cải thiện. Cần GPU.
\item \textbf{Chạy 3 seed} cho cấu hình thắng, vì mọi số Phase 3 hiện là một seed trong khi Phase 2
dùng 3 seed. Không được trộn hai bộ số trong cùng một bảng.
\item \textbf{Quy công giữa hai nhánh} --- do mô hình chỉ có hai nhánh nên giá trị Shapley tính được
chính xác tuyệt đối chỉ với hai lượt suy luận thêm. Trả lời được nhánh nào quyết định kết quả.
\item \textbf{Sửa số liệu trong luận văn} --- câu ở phần tóm tắt hiện ghi ``Mean F1 macro từ 0{,}343
lên 0{,}474''; 0{,}343 thực chất là Severe F1, còn 0{,}474 không khớp tệp số liệu nào. Số đúng theo
3 seed là \textbf{0{,}4202 $\to$ 0{,}5265}, Severe Recall \textbf{12{,}3\% $\to$ 48{,}6\%}.
\item \textbf{Hoàn thiện phần mềm} --- quay video minh hoạ, và xử lý phản hồi của thầy.
\end{enumerate}

\end{document}
