% MIWAI 2026, paper 122. One-page graphical abstract.
%
% This is a rebuild, not a revision. The previous version was three equal
% rounded cards labelled THE PROBLEM / THE MODEL / THE RESULT. That skeleton is
% the clinical "visual abstract" invented at Annals of Surgery in 2016 for
% tweeting trial results: population, intervention, outcome. It belongs to
% clinical-trial journals talking to a lay audience. This is an AI methods
% paper at an LNAI venue, where the real convention is the CVPR-style teaser
% and the Elsevier single-canvas graphical abstract: one continuous surface,
% no card frames, no chrome, the mechanism drawn rather than asserted.
%
% So: one canvas. The scan anchors it, the pipeline is drawn as a pipeline, and
% the headline sits on an axis next to the baseline it beats, because a number
% on an axis demonstrates and a number in a coloured box only asserts.
%
% Colour is the one deliberate constraint. Radiology is greyscale, so the page
% is greyscale, and the single accent is reserved entirely for the Severe class
% and its recall. Nothing else on the page is coloured. The two branches are
% told apart by solid against dashed, which encodes the actual difference
% (trained against frozen) instead of decorating it.
%
% Type is Charter, a printed-text serif, with Avenir Next Condensed only for
% annotation on and around the image, the way a viewer overlays a scan.
%
% Build (needs lualatex for system fonts):
%   lualatex -interaction=nonstopmode infographic.tex
%   pdftoppm -png -scale-to-x 1920 -scale-to-y 1080 infographic.pdf infographic
\documentclass[12pt]{article}
\usepackage[paperwidth=320mm,paperheight=180mm,margin=0mm]{geometry}
\usepackage{fontspec}
\setmainfont{Charter}
\newfontfamily\cond{Avenir Next Condensed}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\pagestyle{empty}
\setlength{\parindent}{0pt}

\definecolor{paper}{HTML}{F7F7F5}
\definecolor{ink}{HTML}{000000}
\definecolor{ink70}{HTML}{4D4D4D}
\definecolor{rule}{HTML}{B8B8B2}
\definecolor{track}{HTML}{DFDFDA}
\definecolor{track2}{HTML}{BFBFB9}
\definecolor{signal}{HTML}{C1440E}   % the Severe class, and nothing else

\begin{document}
\begin{tikzpicture}[x=1mm,y=1mm,
  box/.style={draw=ink, line width=0.4mm, fill=white, minimum height=20mm,
              align=center, inner sep=2mm, font=\fontsize{11}{13}\selectfont},
  frz/.style={draw=ink70, line width=0.4mm, dash pattern=on 1.4mm off 1.1mm,
              fill=white, minimum height=20mm, align=center, inner sep=2mm,
              font=\fontsize{11}{13}\selectfont},
  flow/.style={-{Stealth[length=2.2mm,width=1.8mm]}, line width=0.35mm, ink}]

\fill[paper] (0,0) rectangle (320,180);

%% ------------------------------------------------------------- title
\node[anchor=base west, font=\fontsize{24}{27}\selectfont]
     at (16,164) {A Two-Branch Hybrid Model for Lumbar Disc Grading};
\node[anchor=base west, text=ink70, font=\fontsize{10}{12}\selectfont]
     at (16,155) {Ha Trung Kien and Phan Trong Nhan, Ho Chi Minh City University of Technology, Vietnam};
\node[anchor=base east, text=ink70, font=\fontsize{10}{12}\selectfont]
     at (304,155) {MIWAI 2026, paper 122};
\draw[rule, line width=0.2mm] (16,150) -- (304,150);

%% ------------------------------------------------------------- the scan
\node[anchor=south west, inner sep=0] at (16,66)
     {\includegraphics[width=40mm,height=72mm]{mri_sagittal_panel.png}};
\node[anchor=north west, text=ink70, font=\cond\fontsize{9}{11.5}\selectfont]
     at (16,63) {sagittal T2, five levels graded (L1-L2 to L5-S1)};

%% ------------------------------------------------------------- pipeline
% Both branch boxes are set to one width. Letting the label decide made the
% trained branch twice the size of the frozen one, which reads as a hierarchy
% that does not exist: they are parallel.
\node[box, minimum width=46mm] (bxA) at (118,120) {3D ResNet-34 with CBAM\\[2pt]
     {\cond\fontsize{9}{11}\selectfont trained}};
\node[frz, minimum width=46mm] (bxB) at (118,84) {BiomedCLIP\\[2pt]
     {\cond\fontsize{9}{11}\selectfont frozen}};
\node[box, minimum width=46mm, minimum height=14mm] (fus) at (190,102) {fusion head\\[2pt]
     {\cond\fontsize{9}{11}\selectfont 1.18M trained parameters}};

% One volume feeding both branches, drawn as a split.
\draw[ink, line width=0.35mm] (58,102) -- (80,102);
\draw[ink, line width=0.35mm] (80,120) -- (80,84);
\draw[flow] (80,120) -- (bxA.west);
\draw[flow] (80,84) -- (bxB.west);

\draw[ink, line width=0.35mm] (bxA.east) -- (160,120);
\draw[ink, line width=0.35mm] (bxB.east) -- (160,84);
\draw[ink, line width=0.35mm] (160,120) -- (160,84);
\draw[flow] (160,102) -- (fus.west);

\draw[flow] (fus.east) -- (228,102);
\node[anchor=south, text=ink70, font=\cond\fontsize{9}{11}\selectfont]
     at (220.5,103.5) {cosine match};

%% ------------------------------------------------------------- what comes out
\node[anchor=north west, font=\fontsize{13}{16}\selectfont, align=left]
     at (232,114) {The label set is\\an input, not a\\fixed head.};
\node[anchor=north west, text=ink70, font=\fontsize{10}{13}\selectfont, align=left]
     at (232,92) {Each class is a sentence, so\\a new schema is a new set\\
                  of prompts. On eight unseen\\SPIDER labels disc-morphology\\
                  F1 is 0.587, against 0.433\\off the shelf.};

\draw[rule, line width=0.2mm] (16,56) -- (304,56);

%% ------------------------------------------------------------- the result
\node[anchor=north west, font=\fontsize{13}{16}\selectfont, align=left]
     at (16,50) {Severe is 4\% of the data.};
\node[anchor=north west, text=ink70, font=\fontsize{10}{13}\selectfont, align=left]
     at (16,41) {Cross-entropy training\\finds almost none of it.};

% Two bars from a common zero, not one floating range bar: a bar that starts at
% 12.3\% and ends at 48.6\% invites the left edge to be read as zero.
% Full 0 to 100 axis, because a truncated one would flatter the result.
\node[anchor=west, text=ink70, font=\cond\fontsize{9}{11}\selectfont]
     at (152,48) {Severe recall, RSNA 2024, mean of three seeds};

\node[anchor=east, text=ink70, font=\cond\fontsize{9}{11}\selectfont] at (148,38.5) {cross-entropy};
\fill[track] (152,36) rectangle ++(148,5);
\fill[track2] (152,36) rectangle ++(18.2,5);
\node[anchor=west, text=ink70, font=\fontsize{10}{12}\selectfont] at (173,38.5) {12.3\%};

\node[anchor=east, text=signal, font=\cond\fontsize{9}{11}\selectfont] at (148,29.5) {ours};
\fill[track] (152,27) rectangle ++(148,5);
\fill[signal] (152,27) rectangle ++(71.9,5);
\node[anchor=west, text=signal, font=\fontsize{15}{17}\selectfont] at (227,29.5) {48.6\%};

\node[anchor=north east, text=ink70, font=\cond\fontsize{8.5}{10}\selectfont] at (300,25) {100\%};

%% ------------------------------------------------------------- provenance
\node[anchor=base west, text=ink70, font=\fontsize{8.5}{11}\selectfont]
     at (16,14) {RSNA 2024 and SPIDER. Normal and Mild are pooled; the Severe share is the mean of the three sagittal conditions.};
\node[anchor=base west, text=ink70, font=\fontsize{8.5}{11}\selectfont]
     at (16,9) {Zero-shot is one frozen model scored on the full SPIDER cohort, 1{,}439 intervertebral discs. Imaging from RSNA 2024, used under its non-commercial research licence.};

\end{tikzpicture}
\end{document}
