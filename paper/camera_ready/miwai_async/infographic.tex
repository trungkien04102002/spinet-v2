% MIWAI 2026, paper 122. One-page graphical abstract.
%
% This is a rebuild, not a revision. The previous version was three equal
% rounded cards labelled THE PROBLEM / THE MODEL / THE RESULT. That skeleton is
% the clinical "visual abstract" invented at Annals of Surgery in 2016 for
% tweeting trial results: population, intervention, outcome. It belongs to
% clinical-trial journals talking to a lay audience. This is an AI methods
% paper at an LNAI venue, where the real convention is the CVPR-style teaser
% and the Elsevier single-canvas graphical abstract: one continuous surface,
% no card frames, no chrome, the mechanism drawn rather than asserted.
%
% So: one canvas. The scan anchors it, the pipeline is drawn as a pipeline, and
% the headline sits on an axis next to the baseline it beats, because a number
% on an axis demonstrates and a number in a coloured box only asserts.
%
% Colour is the one deliberate constraint. Radiology is greyscale, so the page
% is greyscale, and the single accent is reserved entirely for the Severe class
% and its recall. Nothing else on the page is coloured. The two branches are
% told apart by solid against dashed, which encodes the actual difference
% (trained against frozen) instead of decorating it.
%
% Type is Charter, a printed-text serif, with Avenir Next Condensed only for
% annotation on and around the image, the way a viewer overlays a scan.
%
% Build (needs lualatex for system fonts):
%   lualatex -interaction=nonstopmode infographic.tex
%   pdftoppm -png -scale-to-x 1920 -scale-to-y 1080 infographic.pdf infographic
\documentclass[12pt]{article}
\usepackage[paperwidth=320mm,paperheight=180mm,margin=0mm]{geometry}
\usepackage{fontspec}
\setmainfont{Charter}
\newfontfamily\cond{Avenir Next Condensed}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\pagestyle{empty}
\setlength{\parindent}{0pt}

% The first palette used warm, olive-tinted neutrals (F7F7F5, DFDFDA, B8B8B2)
% with a brick accent. Tinted neutrals read as dirty rather than as restrained,
% and the whole page came out dusty. These are clean, faintly cool greys, which
% is what reads as clinical, and the accent now has real chroma.
\definecolor{paper}{HTML}{F8F9FA}
\definecolor{ink}{HTML}{000000}
\definecolor{ink70}{HTML}{55595E}
\definecolor{rule}{HTML}{C9CDD2}
\definecolor{track}{HTML}{E3E7EB}
\definecolor{track2}{HTML}{9AA3AC}
% Matches the Severe red of the paper's own class-distribution figure, so the
% two artefacts agree. Used for the Severe class and its recall, nothing else.
\definecolor{signal}{HTML}{E23B3B}
\definecolor{slot}{HTML}{E3E7EB}
% A second accent, earning its place: it marks everything that is actually
% trained. Blue path against grey dashed frozen blocks is the paper's headline
% about parameter efficiency, drawn instead of stated.
\definecolor{trained}{HTML}{1971C2}
\definecolor{trainedbg}{HTML}{EDF4FC}

\begin{document}
\begin{tikzpicture}[x=1mm,y=1mm,
  box/.style={draw=trained, line width=0.45mm, fill=trainedbg, minimum height=20mm,
              align=center, inner sep=2mm, font=\fontsize{11}{13}\selectfont},
  frz/.style={draw=ink70, line width=0.4mm, dash pattern=on 1.4mm off 1.1mm,
              fill=white, minimum height=20mm, align=center, inner sep=2mm,
              font=\fontsize{11}{13}\selectfont},
  flow/.style={-{Stealth[length=2.2mm,width=1.8mm]}, line width=0.35mm, ink},
  tflow/.style={-{Stealth[length=2.2mm,width=1.8mm]}, line width=0.45mm, trained}]

\fill[paper] (0,0) rectangle (320,180);

%% ------------------------------------------------------------- title
\node[anchor=base west, font=\fontsize{24}{27}\selectfont]
     at (16,164) {A Two-Branch Hybrid Model for Lumbar Disc Grading};
\node[anchor=base west, text=ink70, font=\fontsize{10}{12}\selectfont]
     at (16,155) {Ha Trung Kien and Phan Trong Nhan, Ho Chi Minh City University of Technology, Vietnam};
\node[anchor=base east, text=ink70, font=\fontsize{10}{12}\selectfont]
     at (304,155) {MIWAI 2026, paper 122};
\draw[rule, line width=0.2mm] (16,150) -- (304,150);

%% ------------------------------------------------------------- the scan
\node[anchor=south west, inner sep=0] at (16,83)
     {\includegraphics[width=36mm,height=65mm]{mri_sagittal_panel.png}};
\node[anchor=north west, text=ink70, font=\cond\fontsize{9}{11.5}\selectfont]
     at (16,81) {sagittal T2, five levels (L1-L2 to L5-S1)};

%% ------------------------------------------------------------- pipeline
\node[box, minimum width=54mm] (bxA) at (99,128) {3D ResNet-34 with CBAM\\[2pt]
     {\cond\fontsize{9}{11}\selectfont trained}};
\node[frz, minimum width=54mm] (bxB) at (99,100) {BiomedCLIP\\[2pt]
     {\cond\fontsize{9}{11}\selectfont frozen}};
\node[box, minimum width=44mm, minimum height=13mm] (fus) at (165,114) {fusion head};

\draw[ink, line width=0.35mm] (54,114) -- (64,114);
\draw[ink, line width=0.35mm] (64,128) -- (64,100);
\draw[tflow] (64,128) -- (bxA.west);
\draw[flow] (64,100) -- (bxB.west);

\draw[trained, line width=0.45mm] (bxA.east) -- (134,128);
\draw[ink, line width=0.35mm] (bxB.east) -- (134,100);
\draw[trained, line width=0.45mm] (134,128) -- (134,114);
\draw[ink70, line width=0.35mm] (134,114) -- (134,100);
\draw[tflow] (134,114) -- (fus.west);
\draw[tflow] (fus.east) -- (200,114);
\node[anchor=south, text=ink70, font=\cond\fontsize{9}{11}\selectfont]
     at (196,116) {cosine match};

%% ---------------------------------------------- what the classifier actually is
% The paper's most distinctive idea is that the classifier is not a weight
% matrix but a list of sentences. Earlier this block listed three labels with a
% bare number column and the word "slot", which explained neither what was
% being swapped nor what the number was. Now a caret ties the highlighted
% phrase to the instruction, and the numbers carry their own heading.
\node[anchor=base west, font=\fontsize{13}{16}\selectfont]
     at (204,136) {Each class is a sentence.};
\node[anchor=base west, font=\fontsize{10.5}{13}\selectfont] at (204,125)
     {``a magnetic resonance image of \colorbox{slot}{\strut lumbar disc bulging}''};
\draw[trained, line width=0.35mm] (274,122.5) -- (274,120.5);
\node[anchor=base east, text=trained, font=\cond\fontsize{9}{11}\selectfont]
     at (304,117.5) {change this phrase, and nothing is retrained};

\node[anchor=base west, text=ink70, font=\cond\fontsize{9}{11}\selectfont]
     at (204,107) {F1 on two labels the model was never trained on};
\node[anchor=base west, font=\fontsize{9.5}{12}\selectfont] at (210,99) {lumbar disc bulging};
\node[anchor=base east, font=\fontsize{9.5}{12}\selectfont] at (304,99) {0.613};
\node[anchor=base west, text=ink70, font=\fontsize{9.5}{12}\selectfont] at (210,92) {spondylolisthesis};
\node[anchor=base east, text=ink70, font=\fontsize{9.5}{12}\selectfont] at (304,92) {0.028};

\draw[rule, line width=0.2mm] (16,72) -- (304,72);

%% ------------------------------------------------------------- the result
\node[anchor=base west, font=\fontsize{13}{16}\selectfont]
     at (16,64) {Severe is 4\% of the data.};
% Shown, not only stated: the whole training set as one bar, Severe at the end.
\fill[track] (16,52) rectangle ++(94,5);
\fill[track2] (16,52) rectangle ++(90.2,5);
\fill[signal] (106.2,52) rectangle ++(3.8,5);
\node[anchor=base west, text=ink70, font=\cond\fontsize{8.5}{10}\selectfont]
     at (16,47) {Normal, Mild and Moderate};
\node[anchor=base east, text=signal, font=\cond\fontsize{8.5}{10}\selectfont]
     at (110,47) {Severe, 4\%};
\node[anchor=base west, text=ink70, font=\fontsize{10}{13}\selectfont, align=left]
     at (16,33) {Cross-entropy training finds\\almost none of it.};

% A divider, because the left bar is a share of the data and the right bars are
% a detection rate. Two unlike quantities in the same visual idiom read as one
% chart unless something says they are not.
\draw[rule, line width=0.2mm] (128,32) -- (128,66);

\node[anchor=base west, text=ink70, font=\cond\fontsize{9}{11}\selectfont]
     at (152,64) {Severe recall, RSNA 2024, mean of three seeds};

\node[anchor=base east, text=ink70, font=\cond\fontsize{9}{11}\selectfont] at (148,54.5) {cross-entropy};
\fill[track] (152,52) rectangle ++(148,5);
\fill[track2] (152,52) rectangle ++(18.2,5);
\node[anchor=base west, text=ink70, font=\fontsize{10}{12}\selectfont] at (173,54) {12.3\%};

\node[anchor=base east, text=signal, font=\cond\fontsize{9}{11}\selectfont] at (148,42.5) {ours};
\fill[track] (152,40) rectangle ++(148,5);
\fill[signal] (152,40) rectangle ++(71.9,5);
\node[anchor=base west, text=signal, font=\fontsize{15}{17}\selectfont] at (227,41.5) {48.6\%};

\node[anchor=base east, text=ink70, font=\cond\fontsize{8.5}{10}\selectfont] at (300,35) {100\%};
\node[anchor=base west, text=ink70, font=\cond\fontsize{8.5}{10}\selectfont] at (152,35) {0};

%% ------------------------------------------------------------- provenance
\node[anchor=base west, text=ink70, font=\fontsize{8.5}{11}\selectfont]
     at (16,20) {RSNA 2024 and SPIDER. Normal and Mild are pooled; the Severe share is the mean of the three sagittal conditions.};
\node[anchor=base west, text=ink70, font=\fontsize{8.5}{11}\selectfont]
     at (16,15) {Zero-shot is one frozen model scored on the full SPIDER cohort, 1{,}439 intervertebral discs. Imaging from RSNA 2024, used under its non-commercial research licence.};

\end{tikzpicture}
\end{document}
